# Claude Token Analyzer - No external dependencies required for runtime
# This project uses only Python standard library for core functionality

# Development/Testing dependencies (optional)
pytest>=7.0
pytest-benchmark>=4.0
pytest-cov>=4.0
black>=23.0
ruff>=0.1.0
mypy>=1.0
